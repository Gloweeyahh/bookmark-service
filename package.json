{
  "name": "bookmark-service",
  "version": "1.0.0",
  "private": true,
  "description": "A small HTTP API for saving bookmarks, built to handle bad input without crashing.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
